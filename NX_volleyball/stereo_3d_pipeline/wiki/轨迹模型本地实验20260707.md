# 轨迹模型本地实验 2026-07-07

## 数据和环境

- 数据: `test_logs/fullres_zoom_xfeat_20260705_132756/fullres_artifacts_xfeat_20260705_132756.csv`
- sidecar: 同名 `.p2_diagnostic.csv`
- 场景: 静止排球历史片段，未记录真实 `known_z`
- 帧数: 507
- FPS: 约 89fps
- 匹配路径: 507/507 direct pair
- PyTorch 环境: `.venv-stereo-neural/bin/python`

本轮 metadata 只标记 `static: true`，不填 `known_z`，因此所有绝对深度 bias 结论都不能成立；这里只比较静止稳定性、候选一致性和模型是否发生无监督漂移。

## 原始候选质量

| 字段 | 有效率 | mean | std | MAD | 备注 |
|---|---:|---:|---:|---:|---|
| `z_bbox_center` | 507/507 | 3.5269m | 0.0010m | 0.0007m | 静止稳定性最好 |
| `z_circle_center` | 507/507 | 3.5483m | 0.0052m | 0.0034m | legacy `z_stereo` 当前等于它 |
| `z_roi_edge_centroid` | 507/507 | 3.5223m | 0.0033m | 0.0021m | 稳定 |
| `z_roi_edge_pair_center` | 507/507 | 3.5483m | 0.0168m | 0.0085m | 该片段波动偏大 |
| `z_roi_multi_point` | 439/507 | 3.4818m | 0.0055m | 0.0081m | 覆盖 86.6%，相对 P0 偏低 |
| `z_roi_center_patch` | 507/507 | 3.5909m | 0.0051m | 0.0018m | 稳但偏高 |
| `z_roi_cuda_template_match` | 76/507 | 3.4743m | 0.0190m | 0.0009m | low-rate sidecar，部分离群导致 std 大 |
| `z_roi_neural_xfeat` | 39/507 | 3.5064m | 0.0090m | 0.0038m | low-rate sidecar |
| `z_roi_neural_superpoint` | 168/507 | 3.4618m | 0.0138m | 0.0082m | low-rate sidecar |

结论: 该静止片段里最稳的单字段仍是 `z_bbox_center`，但它是否绝对正确必须等已知距离数据验证。P1/P2 候选都有明显相对偏置，适合作为模型输入，但不能直接替代物理滤波输出。

## 候选一致性诊断

新增 `trajectory_fusion/analyze_candidate_consistency.py`，用于统计候选相对 reference 的残差和候选两两共现偏置。当前历史片段没有 `known_z`，所以 `--reference auto` 自动退化为 P0 median。这个结果只能说明候选之间的相对偏置，不能证明哪个字段的绝对深度正确。

| 方法 | 有效率 | median z | 相对 P0 median 残差中位数 | 残差 MAD | 95% 绝对残差 | 判断 |
|---|---:|---:|---:|---:|---:|---|
| `z_bbox_center` | 507/507 | 3.5269m | -1.68cm | 0.55cm | 2.92cm | 稳定，但低于 P0 median |
| `z_circle_center` | 507/507 | 3.5484m | +0.19cm | 0.19cm | 2.02cm | 接近 P0 median |
| `z_roi_edge_pair_center` | 507/507 | 3.5447m | 0.00cm | 0.18cm | 3.02cm | 是该片段 P0 median 的主要锚之一 |
| `z_roi_multi_point` | 439/507 | 3.4784m | -6.23cm | 0.44cm | 6.99cm | 稳定系统偏低，应由模型学习 bias |
| `z_roi_cuda_template_match` | 76/507 | 3.4633m | -7.49cm | 1.06cm | 8.90cm | 覆盖低，作为稀疏观测输入 |
| `z_roi_neural_xfeat` | 39/507 | 3.5064m | -3.66cm | 0.75cm | 5.99cm | 覆盖低，偏置小于 NCC/multi-point |
| `z_roi_neural_superpoint` | 168/507 | 3.4661m | -7.80cm | 0.91cm | 10.75cm | 该历史组合不建议作为默认 |

关键 pairwise 偏置:

- `z_bbox_center - z_circle_center`: -2.12cm，说明 bbox 中心和圆心/径向类存在稳定相对偏差。
- `z_bbox_center - z_roi_edge_centroid`: +0.48cm，两个轻量几何候选相对接近。
- `z_bbox_center - z_roi_center_patch`: -6.21cm，center patch 在该片段明显偏高。
- `z_circle_center - z_roi_radial_center`: 0.00cm，两者在该片段等价。

这个诊断支持当前模型结构: 不要硬选单一字段，也不要把 P1/P2 因相对偏置直接丢弃；应把每路候选、support/std/confidence/top5 和相对残差交给 ReliabilityNet 学 `bias/sigma/outlier_prob`，再由物理 smoother/EKF 输出最终轨迹。

## 非神经校准 baseline

新增 `fit_method_calibration.py` 和 `evaluate_calibrated_smoother.py`，用于明天 known-distance 数据到位后先建立一个强 baseline:

```text
train split known_z
  -> 每种深度方法 median bias / sigma
  -> z_corrected = z_raw - bias
  -> 分组鲁棒观测
  -> robust_smoother 物理轨迹
```

当前历史片段没有 `known_z`，不能拟合这个 calibration；CLI 在没有可标定方法时会返回非零，避免把无标签数据误当绝对标定。这个 baseline 的价值是给 ReliabilityNet 提供对照: 如果固定 bias/sigma 在 heldout 静态距离上已经足够，后续神经模型主要学习动态 outlier 和时序可靠性；如果 fixed calibration 在不同距离/位置上失效，再让 ReliabilityNet 学非线性/场景相关 bias。

## 模型方案对比

| 方案 | smooth mean | smooth std | peak-to-peak | dz RMS | raw std ratio | 判断 |
|---|---:|---:|---:|---:|---:|---|
| raw legacy `z` | 3.4037m | 0.0432m | 0.3365m | 0.0072m | 1.000 | 在线原始输出抖动大 |
| robust smoother, 只用候选深度 | 3.5239m | 0.0021m | 0.0154m | 0.0010m | 0.049 | 当前最佳本地 baseline |
| robust smoother + 在线 `x/y/z` | 3.4957m | 0.0102m | 0.0796m | 0.0011m | 0.237 | 不建议，legacy 在线状态会拉坏深度 |
| ReliabilityNet h32/e80/static0.5 | 3.4973m | 0.0058m | 0.0477m | 0.0057m | 0.134 | 可跑，但没有后级滤波 |
| ReliabilityNet h32/e160/static5 | 3.2132m | 0.0138m | 0.2156m | 0.0066m | 0.320 | 发生无标签漂移 |
| ReliabilityNet h64/e120/static1 | 3.6395m | 0.0120m | 0.1350m | 0.0067m | 0.277 | 发生无标签漂移 |
| ReliabilityNet h32/e120/static1/bias0.2 | 3.3740m | 0.0105m | 0.1475m | 0.0071m | 0.243 | bias 正则不足 |
| ReliabilityNet h32/e120/static1/bias1.0 | 3.4738m | 0.0053m | 0.0742m | 0.0035m | 0.122 | 比无正则稳，但仍不如物理滤波 |

## ReliabilityNet 加权物理平滑

新增 `trajectory_fusion/evaluate_reliability_smoother.py`，用于验证推荐架构:

```text
ReliabilityNet 输出 per-method bias/sigma/outlier_prob
  -> z_corrected = z_raw - bias
  -> variance = sigma^2 / inlier_prob
  -> 按方法族分组，保留族内分歧为额外方差
  -> robust_smoother 物理状态更新
```

这条链路和 `evaluate_reliability_checkpoint.py` 的区别是: 模型不再直接输出最终 `smooth_z`，只影响每路观测的偏置修正和方差，最终轨迹仍由物理滤波器生成。

防标签泄漏约束: `robust_smoother.py`、`evaluate_reliability_smoother.py` 和 `run_evaluation_suite.py` 默认都不把静态 `known_z` 当作 smoother update。`known_z` 只用于 `check_dataset` / `evaluate_fusion` 计算 bias/MAD。只有生成标签辅助伪真值时才显式加 `--use-static-known-z`。

| 方案 | smooth mean | smooth std | peak-to-peak | raw std ratio | 判断 |
|---|---:|---:|---:|---:|---|
| robust smoother, 手工 sigma | 3.5239m | 0.0021m | 0.0154m | 0.049 | 当前未知真值 baseline |
| ReliabilityNet direct h32/e120/bias1.0 | 3.4738m | 0.0053m | 0.0742m | 0.122 | 直接 consensus，仍有漂移 |
| reliability smoother h32/e80 | 3.4975m | 0.0046m | 0.0333m | 0.107 | learned sigma 接物理层，可跑 |
| reliability smoother h32/e120/bias1.0 | 3.4737m | 0.0040m | 0.0478m | 0.092 | 高频抖动受控，但继承 checkpoint 绝对偏置 |
| reliability smoother h32/e120/bias0.2 | 3.3737m | 0.0093m | 0.1269m | 0.215 | bias 约束不足，仍漂移 |
| reliability smoother h32/e160/static5 | 3.2128m | 0.0138m | 0.2181m | 0.319 | 训练目标欠约束 |
| reliability smoother h64/e120/static1 | 3.6393m | 0.0110m | 0.1122m | 0.255 | 训练目标欠约束 |
| reliability smoother h32/e80/bias1.0/leave0.02 | 3.5550m | 0.0103m | 0.1031m | 0.238 | 单段无真值下留一法仍漂移 |
| reliability smoother h32/e80/bias1.0/leave0.10 | 3.6462m | 0.0075m | 0.1336m | 0.173 | 留一法权重过大会把最高权重推到单一几何候选 |

诊断字段已写入输出 CSV:

- `reliability_smoother_valid_count`
- `reliability_smoother_group_count`
- `reliability_smoother_top_method`
- `reliability_smoother_top_method_name`
- `reliability_smoother_top_weight`
- `reliability_smoother_top_raw_z`
- `reliability_smoother_top_corrected_z`
- `reliability_smoother_top_bias`
- `reliability_smoother_top_sigma`
- `reliability_smoother_top_inlier_prob`

本轮 `h32/e120/bias1.0` 的最高权重主要落在 `bbox_left_edge`，平均每帧约 5-6 个方法族观测。这说明当前无真值 checkpoint 只适合验证管线，不适合作为最终可靠性模型。

`--leave-one-weight` 已接入训练脚本，但在当前单段无真值静止数据上表现更差: `leave0.02` 最高权重几乎全部落到 `bbox_right_edge`，`leave0.10` 最高权重几乎全部落到 `roi_radial_center`。后续只建议在多距离 known_z、heldout split 和动态片段齐全后作为 sweep 配置测试，不作为当前默认。

## 关键结论

1. 当前最可靠的本地方案是“候选方法分组 + 鲁棒物理 smoother”，不是单独 GRU consensus。
2. ReliabilityNet 的职责仍应是输出每路候选的 `bias/sigma/outlier_prob`，最终轨迹必须交给 EKF/UKF/RTS 或当前 `robust_smoother` 类物理滤波器。
3. 没有真实 `known_z`、平面、落点或重投影弱标签时，纯自监督 ReliabilityNet 会出现绝对深度漂移。`static_jitter` 只能让曲线更平滑，不能保证深度正确。
4. 已在训练脚本中加入 `--bias-reg-weight` 和 `bias_regularizer()`，用于抑制无标签阶段的全方法 bias 漂移。它只能降低风险，不能替代真实距离标签。
5. 明天采集数据时，必须至少覆盖:
   - 静止已知距离，多个距离点。
   - 静止不同画面位置，验证 bbox/circle anchor 的系统偏差。
   - 轻微上下/前后移动，验证速度/加速度特征。
   - 抛球/击球，验证物理轨迹残差和落点预测。

## 推荐下一阶段模型结构

```text
原始候选 z/disparity/support/std/confidence/top5/pair gate/dy
  -> MeasurementReliabilityNet
     输出每路 bias/sigma/outlier_prob
  -> 分组鲁棒观测构造
  -> EKF/UKF/RTS 物理轨迹滤波
  -> x/y/z/v/a 和落点预测
```

不要把 legacy 在线 `x/y/z/v/a` 作为 ReliabilityNet 的训练输入；本轮 `--use-online-position` 已显示它会把深度稳定性拉差。后续若要加入 `xy/v/a`，应由每个原始候选自身反投影和短窗差分生成，而不是复用旧在线融合状态。

## 复现实验命令

```bash
CSV=NX_volleyball/stereo_3d_pipeline/test_logs/fullres_zoom_xfeat_20260705_132756/fullres_artifacts_xfeat_20260705_132756.csv
META=NX_volleyball/stereo_3d_pipeline/test_logs/trajectory_model_experiments_20260707/fullres_artifacts_xfeat_static.metadata.yaml
OUT=NX_volleyball/stereo_3d_pipeline/test_logs/trajectory_model_experiments_20260707

python3 NX_volleyball/stereo_3d_pipeline/trajectory_fusion/check_dataset.py "$CSV"

python3 NX_volleyball/stereo_3d_pipeline/trajectory_fusion/robust_smoother.py \
  "$CSV" --metadata "$META" --gravity-y 0 \
  -o "$OUT/fullres_artifacts_xfeat_robust_smooth.csv"

.venv-stereo-neural/bin/python NX_volleyball/stereo_3d_pipeline/trajectory_fusion/train_reliability.py \
  "$CSV" --metadata "$META" --epochs 120 --hidden 32 \
  --static-jitter-weight 1.0 --bias-reg-weight 1.0 \
  --known-z-weight 0 --known-z-range-weight 0 \
  -o "$OUT/reliability_static_h32_e120_j1_bias10.pt"

.venv-stereo-neural/bin/python NX_volleyball/stereo_3d_pipeline/trajectory_fusion/evaluate_reliability_checkpoint.py \
  "$OUT/reliability_static_h32_e120_j1_bias10.pt" "$CSV" \
  --metadata "$META" \
  -o "$OUT/fullres_artifacts_xfeat_reliability_h32_e120_j1_bias10.csv"

.venv-stereo-neural/bin/python NX_volleyball/stereo_3d_pipeline/trajectory_fusion/evaluate_reliability_smoother.py \
  "$OUT/reliability_static_h32_e120_j1_bias10.pt" "$CSV" \
  --metadata "$META" --gravity-y 0 \
  -o "$OUT/fullres_artifacts_xfeat_reliability_smoother_h32_e120_j1_bias10.csv" \
  --json-out "$OUT/reliability_smoother_h32_e120_j1_bias10_apply.json"
```
