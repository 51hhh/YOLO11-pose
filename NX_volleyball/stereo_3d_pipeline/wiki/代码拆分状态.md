# 代码拆分状态

最后核对: 2026-07-04

本页只记录单文件膨胀治理的当前状态。当前范围已收窄为 C++ 实时流水线和相关 C++/CUDA 文件；Python 脚本、离线工具和训练脚本的拆分已回滚，不再作为本轮拆分目标。

当前扫描: C++/CUDA 最大文件仍在实时 pipeline、CUDA depth candidate、TensorRT detector 和相机/显示工具。涉及编译环境、GPU stream、deadline 或设备 SDK 的改动必须在 NX 上验证；无 NX 时只做纯 helper、数据结构和无状态转换拆分。

## 已拆分

| 区域 | 当前边界 |
|---|---|
| `src/utils/trajectory_recorder.cpp` | 保留录制配置、文件生命周期和异步队列入口；主 CSV header/row、writer loop 和 flush 已拆到 `trajectory_recorder_writer.cpp`；`.frames.csv` 表头、逐帧统计和 sidecar 行写出已拆到 `trajectory_recorder_summary.*` |
| `src/utils/baseline_clip_recorder.cpp` | 保留采集状态机、VPI/CUDA 图像 copy 和队列；文件名、format/mode 归一化、`frames.csv` 表头、metadata 和检测列写出已拆到 `baseline_clip_recorder_io.*`；writer loop、落图和逐帧 `frames.csv` 追加已拆到 `baseline_clip_recorder_writer.cpp` |
| `src/main_config_loaders.cpp` | 保留顶层 `PipelineConfig` YAML 解析和配置域分发；`detector.dual_yolo` 字段映射已拆到 `main_dual_yolo_config.*`；预测、轨迹录制、baseline clip 和 realtime debug dump 配置解析已拆到 `main_recording_config_loaders.cpp` |
| `src/main.cpp` | 保留入口编排、pipeline 初始化、callback 绑定和退出流程；CLI 解析在 `main_cli_options.*`，baseline/realtime debug CLI 覆盖和 baseline pipeline 降级在 `main_runtime_overrides.*`，显示、调试 dump、配置加载已拆到对应 helper/source |
| `src/main_display_helpers.h` | 保留显示 helper 兼容聚合入口；点击测距/display job 类型在 `main_display_types.h`，VPI/CUDA frame 抓取和 color remap 在 `main_display_capture.h`，检测框/深度/预测/HUD 绘制在 `main_display_render.h` |
| `src/main_realtime_debug_dump.h` | 只保留 realtime debug dump 配置、job 和 dumper 接口；采样、队列、VPI gray copy 和生命周期在 `main_realtime_debug_dump.cpp`；zoom 绘制、JSON/PNG 写出和 writer loop 在 `main_realtime_debug_dump_writer.cpp` |
| `src/calibration/stereo_calibrate.cpp` | 保留标定主流程、误差报告、结果保存和可视化预览；CLI 解析在 `stereo_calibrate_args.*`，图像枚举/配对、灰度预处理和棋盘角点检测在 `stereo_calibrate_images.*` |
| `src/stereo_depth_viewer.cpp` | 保留相机/VPI 初始化、采集循环和算法调度；CLI 在 `stereo_depth_viewer_args.*`，headless 报告在 `stereo_depth_viewer_benchmark.*`，ONNX 模型查找加载在 `stereo_depth_viewer_models.*`，显示模式/OSD/鼠标状态在 `stereo_depth_viewer_ui.*`，viewer 视差/深度后处理在 `stereo_depth_viewer_depth_utils.h` |
| `src/calibration/capture_chessboard.cpp` | 保留海康双目采集、PWM 启停、预览、落图和 metadata 写出；CLI 解析和参数校验在 `capture_chessboard_args.*` |
| `tools/hik_frame_metadata_probe.cpp` | 保留海康 SDK 枚举、相机配置和采集；CLI 解析在 `hik_frame_metadata_probe_args.*`，metadata 统计/打印在 `hik_frame_metadata_probe_stats.*` |
| `src/fusion/hybrid_depth.cpp` | 保留 mono/stereo 融合、IoU track 匹配和 fallback 观测权重；9D Kalman track 在 `hybrid_depth_track.cpp`，ROI 候选字段透传在 `hybrid_depth_candidate_copy.*` |
| `src/fusion/trajectory_predictor.cpp` | 保留弹道预测、polynomial fallback 调用和 prediction 选择；二次/线性最小二乘拟合已拆到 `trajectory_predictor_fit.cpp`；per-track history 管理已拆到 `trajectory_predictor_history.cpp` |
| `src/stereo/roi_geometry_cpu.cpp` | 保留 CPU 点型几何候选；圆拟合、bbox proxy 和 fallback 极线圆搜索在 `roi_geometry_circle_cpu.cpp` |
| `src/stereo/roi_feature_match_cpu_helpers.h` | 保留 sparse/ROI normalization/debug 公共 helper；OpenCV extractor、descriptor score 和 detect/describe 在 `roi_feature_match_opencv_helpers.h` |
| `src/stereo/depth_candidate_builder.cpp` | 保留候选 anchor 计算、P0 几何/bbox 候选追加和 legacy 选择；P1/P2 feature、CUDA、neural 和 fallback 候选追加已拆到 `depth_candidate_builder_feature.*` |
| `src/stereo/depth_match_contract.cpp` | 保留左右 YOLO pair gate 和 reject 统计；候选方法/source/legacy allow-list 在 `depth_candidate_contract.cpp` |
| `src/pipeline/pipeline_dual_yolo_match.cpp` | GPU refine pair 收集、bbox disparity penalty、排序、裁剪和 `DualYoloGpuDetectionPair` 转换已抽到 `pipeline_roi_match_helpers.*` 的 `buildGpuDetectionPairsForRefine()`；主文件保留 `matchPairs()` 调用、profile 和对象构建 |
| `src/capture/hikvision_camera.cpp` | SDK 节点配置、设备选择、水印清理 helper 已拆到 `hikvision_camera_helpers.h` |
| `src/pipeline/pipeline.h` | 公开 callback 契约已拆到 `pipeline_callbacks.h`；运行配置已拆到 `pipeline_config.h` |
| `src/pipeline/frame_slot.h` | 保留 `FrameSlot`、`FrameMetadata` 和 VPI/CUDA 资源生命周期；`Object3D` 已拆到 `object3d_types.h` |
| ROI/特征匹配部分 | CPU feature helpers、CPU debug、neural direct helper 已部分拆分 |

## 仍偏大的文件

| 文件 | 当前判断 |
|---|---|
| `src/pipeline/pipeline_dual_yolo_match.cpp` | 最大风险点，核心实时匹配函数高度耦合；下一步只适合先抽纯 helper 或 FeatureJob 数据结构，完整调度拆分需要 NX 编译和实测 |
| `src/pipeline/pipeline_async_roi.cpp` | 可按队列/worker/slot copy 分段拆，但必须验证异步 deadline 和丢帧语义 |
| `src/stereo/dual_yolo_depth_gpu_roi_match.cuh` | CUDA ROI candidate 内核集合，适合按候选族拆 header；需要 NX CUDA 编译 |
| `src/stereo/neural_feature_matcher_direct.cpp` | 神经特征 direct path 仍偏大；可继续按 engine binding、ROI preprocess、match postprocess 拆 |
| `src/detect/trt_detector.cpp` | TensorRT lifecycle、preprocess、postprocess 可拆；需要 TensorRT 编译环境验证 |
| `src/stereo/roi_feature_match_gpu.cpp` | OpenCV CUDA / GPU ROI 特征候选集中；适合按算法族拆，但必须验证 CUDA/OpenCV 链接和 tail latency |
| `src/capture/hikvision_camera.cpp` | 仍包含 SDK 采集和元数据细节；进一步拆需要相机 SDK 实机验证，避免破坏水印/到达时间语义 |
| `tools/hik_frame_metadata_probe.cpp` | CLI 和统计 helper 已拆；剩余是 SDK 元数据探针主流程，继续拆相机配置/采集前需 SDK 编译环境验证 |
| `src/pipeline/pipeline.cpp`、`src/pipeline/pipeline_loops.cpp` | 主循环和运行态 glue 偏大；涉及线程、callback、drop 语义，需 NX 连续运行验证 |
| `src/fusion/hybrid_depth.cpp` | Kalman track 和候选透传已拆；剩余是融合策略主流程，继续拆观测选择/track 生命周期前需用真实 CSV/动态数据回归验证 |
| `src/stereo/dual_yolo_depth_gpu.cu`、`src/stereo/dual_yolo_depth_gpu_device_helpers.cuh` | CUDA device/helper 集中；适合按 P0/P1/P2 candidate family 拆 header/kernel，必须有 NX CUDA 编译和 profile |

## 后续顺序

1. 继续只拆 C++ 实时流水线中可本地审查的纯 helper、数据结构和无状态转换。
2. 涉及实时调度、CUDA stream、`roi_postprocess_mutex_` 或 FeatureJob 的修改，落到架构 TODO 并等待 NX 编译/100fps 实测。
3. 每次拆分单独提交，避免把行为修改和文件整理混在一起。
