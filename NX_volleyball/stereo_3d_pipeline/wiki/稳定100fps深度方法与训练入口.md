# 稳定 100fps 深度方法与训练入口

最后核对: 2026-07-04

本页是“实际选用”结论页，用于冻结当前 100fps 实时深度候选集合，并说明后续标定测试、数据采集和轨迹融合模型训练应如何使用这些候选。深度相关页面总入口见 [深度算法导航](深度算法导航.md)。

轨迹模型、半监督训练、物理约束和数据集设计见 [排球轨迹模型训练方案](排球轨迹模型训练方案.md)。

## 依据

当前结论优先使用单算法隔离矩阵:

```text
test_logs/nx_algorithm_matrix_isolated_20260703_101520/report.md
```

P0+P1 联合录制复核:

```text
/home/nvidia/trajectory_dataset/p0p1_100fps_20260703_135728
```

该片段 `2304` 帧、`23.248s`，目标级 CSV 与 frame sidecar 均为 `2304` 行，实测输出约 `99.06fps`。`Stage2_AsyncRoiWorker` 平均 `4.67ms`、最大 `10.21ms`，只有 `1` 次超过 `10ms` deadline。说明当前 P0+P1 字段可以作为 100fps 采集基线，但新增算法必须作为 P2 isolated A/B，不能叠加进默认全开集合。

历史 `test_logs/nx_algorithm_matrix_20260703_0915/` 是旧脚本输出，默认几何候选没有完全关闭，只能作为历史参考，不能作为单算法有效性的最终依据。

P2 复测依据:

```text
test_logs/codex_p2_after_diag_20260704_045653/
test_logs/codex_p2_diag_only_rebuilt_20260704_050431/
test_logs/codex_diag_csv_verify2_20260704_051834/
test_logs/codex_diag_csv_final_20260704_052610/
```

## P0: 当前可稳定使用

这些方法满足 100fps，候选有效率高，适合进入实时候选字段和训练数据采集。

| 方法 | CSV 字段 | source | FPS | 有效率 | 中位深度 | MAD | 用途 |
|---|---|---:|---:|---:|---:|---:|---|
| YOLO bbox 中心视差 | `z_bbox_center` | `3` | `100.0` | `611/611` | `3.6129m` | `0.0008m` | 最轻量后备和训练候选 |
| ROI 圆心/球心拟合 | `z_circle_center` | `1` | `99.9` | `615/615` | `3.6139m` | `0.0010m` | 当前默认主观测优先项 |
| ROI 边缘质心 | `z_roi_edge_centroid` | `5` | `99.3` | `637/637` | `3.6091m` | `0.0013m` | 稳定几何候选 |
| ROI 径向中心 | `z_roi_radial_center` | `8` | `100.3` | `616/616` | `3.6138m` | `0.0009m` | 稳定几何候选 |
| ROI 成对边缘中心 | `z_roi_edge_pair_center` | `9` | `100.1` | `641/641` | `3.6108m` | `0.0000m` | 稳定几何候选 |

实时采集必须开启 P0；当前主配置同时开启 P1 作为训练候选记录。

```yaml
detector:
  dual_yolo:
    depth_modes:
      bbox_pair: true
      bbox_edges: true
      circle_center: true
      circle_edges: true
      roi_edge_centroid: true
      roi_radial_center: true
      roi_edge_pair_center: true
      roi_center_patch: true
      roi_subpixel: true
      epipolar_fallback: true
    subpixel_enabled: true
```

`epipolar_fallback` 只作为单侧漏检退化保护，不作为正常主路径。当前 YOLO/IoU 退化回归通过，但单侧缺失模板搜索中心误差 p95 约 `11-12px`，训练时应单独标记 `stereo_match_source=2/3`。

## P1: 100fps A/B 候选

这些方法满足或接近 100fps，但不能直接视为稳定主路径。建议在标定测试中作为额外候选记录，用模型学习其偏置和可靠性。

| 方法 | CSV 字段 | FPS | 有效率 | 中位深度 | MAD | 当前判断 |
|---|---|---:|---:|---:|---:|---|
| CUDA multi-point subpixel | `z_roi_multi_point` | `100.1` | `628/628` | `3.5384m` | `0.0000m` | 覆盖率和速度好，但相对 P0 几何候选有约 `7cm` 系统差，需要真实距离验证 |
| CUDA center patch | `z_roi_center_patch` | `100.0` | `45/45` | `3.5384m` | `0.0000m` | 可跑但覆盖率低，不适合作为默认主路径 |

P0+P1 联合录制复核中，`z_roi_multi_point` 为 `2229/2304`，相对 P0 几何中值约 `-5.17cm`；`z_roi_center_patch` 为 `2239/2304`，相对 P0 几何中值约 `+5.30cm`。因此 P1 当前定位是训练候选和偏置学习输入，不作为 legacy 在线主深度。

推荐采集策略:

- 基线验证: 可以只开 P0，用于确认标定、水印、几何一致性和真实距离 bias。
- 训练采集: 当前主配置开 P0 + `roi_subpixel` + `roi_center_patch`，用于记录 P1 偏置和可靠性。
- `roi_center_patch` 单算法历史测试覆盖有限，但 P0+P1 联合录制覆盖率已到 `97%` 左右；它仍有约 `+5cm` 系统偏置，不能作为主路径依赖。

## P2: 默认关闭的待测试/待加速候选

当前冻结 P0+P1 作为默认采集集合。P2 的含义是“有真实 GPU/TensorRT 路线或已接入字段，但还没有通过本项目 NX isolated 100fps 验收”。P2 必须单项开启、单项记录 `Stage2_AsyncRoiWorker`/有效率/深度偏置/长尾耗时，不能叠加进默认 P0+P1 配置，也不能抢占 legacy `z_stereo/obj.z`。

2026-07-04 复测后，只有 `iou_region_color_patch_wide_search` 和 `patch_iou_color_edge_wide_search` 具备继续作为 P2 训练候选长测的价值；其中 color-edge wide 是当前最稳的 P2 颜色/边缘候选。它们仍不进入默认 P0+P1，也不参与 legacy 在线主深度。

每帧 diagnostic-only 运行 OpenCV CUDA ORB/Template/BM/SGM 已证明调度和独立 GPU snapshot 可用，但会和 YOLO 争抢 GPU 并压低整体 FPS；后续只能按 stride 或 P0/P1 异常选择性触发。diagnostic CSV 已可用，见 [P2 算法 NX 测试排查指南](P2算法NX测试排查指南.md)。

### P2 已接入实时字段，当前结论

| 方法 | 字段/输出 | 2026-07-04 结论 | 下一步 |
|---|---|---|---|
| `roi_iou_region_color_patch_wide_search` | `z_roi_iou_region_color_patch` | `100.7fps`、`241/241` 有效，median `3.3250m`，但有 `2` 次 stale | 长测和真实距离标定，只作为训练候选 |
| `roi_patch_iou_color_edge_wide_search` | `z_roi_patch_iou_color_edge` | `99.9fps`、`246/247` 有效，worker p95 `1.79ms`，当前最稳 P2 | 长测和真实距离标定，只作为训练候选 |
| OpenCV CUDA ORB | `z_roi_orb_points` | true `cv::cuda::ORB` + CUDA BF；`84.6fps`、`21/198` 有效，长尾 `30.69ms` | 不进入默认；若继续做，只能 selective/diagnostic |
| OpenCV CUDA TemplateMatching | `z_roi_cuda_template_match` | inline `101.7fps` 但 `0/235` 有效且长尾 `41.38ms`；diagnostic CSV 可记录逐帧结果 | 不进入默认；只保留诊断/参数试验 |
| OpenCV CUDA StereoBM | `z_roi_cuda_stereo_bm` | `98.7fps`、`0/231` 有效 | 不进入默认；优先改 GPU 内聚合/gate |
| OpenCV CUDA StereoSGM | `z_roi_cuda_stereo_sgm` | `100.4fps`、`0/231` 有效，长尾 `12.95ms` | 不进入默认；优先改 GPU 内聚合/gate |
| XFeat TensorRT | `z_roi_neural_feature` | 96 engine trtexec `0.33ms`，但 realtime top32 仅 `6/229` 有效、top64 为 `0/248` | 后处理/NMS/匹配迁到 GPU 前不准入 |
| SuperPoint TensorRT extractor | `z_roi_neural_feature` | top64 engine 可跑，但 realtime `57.9-90.4fps` 且有效率低 | 不准入；需优化后处理和匹配 |
| ALIKED TensorRT extractor | `z_roi_neural_feature` | 当前缺可用 engine；vanilla ONNX 卡在 DCN | 若能得到无 DCN engine 或 TensorRT plugin engine，则按 `160/top64`, `224/top64` 进入同一矩阵 |

### P2 待实现或待引入后端

| 方向 | 建议形态 | 当前判断 |
|---|---|---|
| ROI ring/edge profile matcher | 复用 circle/edge/radial 信息，沿球边界或径向采样，做小范围极线匹配 | 最值得先实现；比通用关键点更贴合排球 |
| masked dense cost volume / SGM-lite | 球 mask 内、`initial_disp ± 小范围` 的 CUDA cost volume，输出 robust median disparity | 可验证能否修正 P1 系统偏置；必须严格限制搜索范围 |
| VPI CUDA Template Matching | `vpiCreateTemplateMatching` + `vpiSubmitTemplateMatching`, NCC | VPI 官方 CUDA 后端可用，适合作为 OpenCV TemplateMatching 对照 |
| VPI CUDA Stereo Disparity | `vpiSubmitStereoDisparityEstimator`, 小 ROI / 小 `maxDisparity` | VPI CUDA 使用 SGM，并输出 confidence map；可作为 OpenCV BM/SGM 对照 |
| Fixstars libSGM | 裁剪 ROI 或小分辨率双目图，4-path/subpixel SGM | 公开 Xavier MAXN 1024x440/128disp 约 `9ms`；ROI 版有希望，但需要引入第三方库 |
| VPI Harris + Pyramidal LK | VPI CUDA Harris 检点，右 ROI 按 initial disparity 对齐后做 LK | 真实 GPU 关键点路线；固定少点数、单层/少层、小窗口 |
| OpenCV CUDA GFTT/Harris + SparsePyrLK | OpenCV CUDA 角点 + LK，小 ROI 小位移 | 若本机 OpenCV CUDA 模块完整，适合作为轻量关键点 A/B |
| VPI ORB | VPI ORB detector/descriptor | 官方文档有 ORB API；需确认 NX 当前 VPI 版本是否支持 CUDA 后端和 descriptor 取回成本 |
| CUDA-SIFT 第三方实现 | CudaSift 等依赖，限制 ROI/top-k | SIFT 概念可进入 P2 依赖试验；当前 OpenCV CPU SIFT 仍只算 debug 对照 |
| BRISK/AKAZE GPU 路线 | 查找第三方 CUDA/VPI 替代或改用 ORB/GFTT+LK | 当前 OpenCV 没有项目已接入的 CUDA BRISK/AKAZE；算法概念保留 P2 调研，不跑 CPU 版当实时结论 |
| CUDA Canny/HoughCircles fallback refinement | 单侧 fallback 命中后，用边缘/圆检测细化中心和半径 | 更适合修正 fallback 中心跳变；不是通用纹理匹配 |

## 候选字段与模型边界

训练口径是: C++ 实时管线负责计算并写出每种深度方法对应的原始字段，包括深度、disparity、support、confidence 和质量诊断；可靠性模型训练时直接读取这些原始候选字段，不读取代码已经择一之后的兼容字段。

当前 C++ 里仍有 legacy `z_stereo` / `z` 输出，用于兼容 ROS、日志和旧轨迹预测。实时管线会通过 `buildDepthCandidateObservations()` 组装完整候选，再由 `selectLegacyDepthOutputCandidate()` 写入 `z_stereo`、`obj.z`、`raw_z` 和 `stereo_depth_source`。这个 legacy 输出只允许 P0 几何/bbox 和退化 fallback，不允许 P1/神经/颜色/传统特征抢占在线主深度。训练模型时不要把 `z_stereo` 或 `z` 当作一个独立深度方法，也不要把它当作标签；它们只能作为 legacy baseline/诊断字段。`trajectory_fusion/dataset.py` 中的 `METHOD_COLUMNS` 已只包含原始候选方法字段。

当前候选构建顺序以 `src/stereo/depth_candidate_builder.cpp` 为准。这个顺序只影响原始候选列表，不代表 legacy 输出选择:

```text
z_roi_multi_point
z_fallback_feature_points
z_fallback_template
z_fallback_epipolar / z_circle_center
z_roi_center_patch
z_roi_iou_region_color_patch
z_roi_patch_iou_color_edge
z_roi_neural_feature
z_roi_corner_points
z_roi_texture_points
z_roi_binary_points
z_roi_orb_points
z_roi_brisk_points
z_roi_akaze_points
z_roi_sift_points
z_roi_radial_center
z_roi_edge_pair_center
z_roi_edge_centroid
z_circle_left_edge
z_circle_right_edge
z_bbox_center
bbox_edges_internal              # 内部边缘候选，CSV 主要看 left/right edge 字段
z_bbox_left_edge
z_bbox_right_edge
```

默认 `config/pipeline_dual_yolo_roi.yaml` 下，P0 和 P1 候选都会计算并写入 CSV；颜色、神经、OpenCV 特征和 CPU fallback 默认关闭。实际允许参与 legacy `z_stereo` 兼容选择的候选为:

```text
z_fallback_epipolar / z_circle_center
z_roi_radial_center
z_roi_edge_pair_center
z_roi_edge_centroid
z_circle_left_edge
z_circle_right_edge
z_bbox_center
bbox_edges_internal
z_bbox_left_edge
z_bbox_right_edge
```

其中 `z_fallback_epipolar` 只在单侧漏检且极线 fallback 有效时排在该槽位；正常左右 YOLO 配对时同一槽位通常是 `z_circle_center`。`z_fallback` 仍作为兼容汇总字段写出。`z_roi_multi_point` 和 `z_roi_center_patch` 会继续写入 CSV，但不会写入 legacy `z_stereo/obj.z`。模型训练和评估必须直接读取原始 P0/P1 字段。

训练采集时可以同时开启 P0/P1 候选，但评估脚本和训练脚本必须直接读取:

```text
z_bbox_center
z_circle_center
z_roi_edge_centroid
z_roi_radial_center
z_roi_edge_pair_center
z_roi_multi_point
z_roi_center_patch
```

而不是读取“代码已选择之后”的 `z_stereo`。

## 标定测试采集要求

每次标定后应先录制静态球和短动态片段，使用 `recording.detail_level: extended`，保留所有 P0/P1 候选字段。

建议静态测试点:

- 距离: `2m`, `3m`, `4m`, `5m`, `6m`
- 横向: 中心、左侧、右侧
- 高度: 低、中、高
- 每个点至少 `5-10s`

必须记录:

- `z_bbox_center`
- `z_circle_center`
- `z_roi_edge_centroid`
- `z_roi_radial_center`
- `z_roi_edge_pair_center`
- P1 训练候选: `z_roi_multi_point`, `z_roi_center_patch`
- `stereo_match_source`, `stereo_depth_source`
- `pair_initial_disparity`, `pair_epipolar_dy`, `pair_shifted_iou`, `pair_score`, `pair_bbox_prior_penalty`
- 左右 bbox/circle 几何字段
- frame counter / trigger / timestamp 水印字段
- `*.frames.csv` sidecar 中的 no-output、fallback 和 pair 质量统计

## 现有模型架构

已有训练入口位于:

```text
trajectory_fusion/
  dataset.py
  models.py
  losses.py
  train_reliability.py
  evaluate_fusion.py
```

当前模型不是直接预测最终轨迹，而是学习每个测量方法的可靠性:

- `MeasurementReliabilityNet`
  - 输入: 每帧候选深度、disparity、support、confidence、bbox/circle、同步水印等特征序列。
  - 主体: `Linear + LayerNorm + SiLU + GRU`。
  - 输出: 每个方法的 `log_sigma`, `bias`, `outlier_logit`，以及公共不确定度。
- `weighted_depth_consensus`
  - 使用模型输出的 sigma/bias/outlier 对多个深度候选做加权融合。
  - 输出融合后的深度 consensus。
- `TinyCausalFusionHead`
  - 可选实时部署实验头，避免 GRU，适合后续 NX 低延迟推理。

损失函数:

- `measurement_consistency_loss`: 候选深度之间的鲁棒一致性。
- `physics_depth_loss`: 深度方向低加速度/低 jerk 先验。
- `uncertainty_regularizer`: 避免模型退化为全大 sigma 或全 outlier。

## 训练输入状态

`trajectory_fusion/dataset.py` 已读取深度候选、support、confidence、同步水印、运动状态和左右配对质量。训练输入中已经加入:

- `pair_initial_disparity`
- `pair_epipolar_dy`
- `pair_y_tolerance`
- `pair_size_ratio`
- `pair_shifted_iou`
- `pair_score`
- `pair_bbox_prior_penalty`
- `pair_positive_disparity`

这些字段用于让模型学习 YOLO/IoU 配对质量，并区分“深度候选本身错”和“左右框配对错”。

模型还需要看到时间连续性。当前 `legacy_feature_names()` 不读取在线 `x/y/z/vx/vy/vz` 状态字段，而是从连续帧候选集合构造 `candidate_median_z`, `candidate_mad_z`, `candidate_valid_count`, `candidate_dz`, `candidate_ddz`，并读取候选深度、质量字段和 pair gate。严格候选-only 训练要继续避免把旧在线 `z_stereo/z` 选择结果泄漏进训练输入。

当前训练脚本已支持 `metadata.yaml` / `--metadata`，并接入 `known_z`, `known_z_min/max`, `static` 弱标签。固定平面、落地区域、重投影和完整物理 rollout loss 仍是下一阶段。

## 推荐训练路线

1. 先用 P0 候选做真实距离静态测试，建立每种方法的 bias/MAD 表。
2. 再加入 P1 的 `roi_multi_point`，判断其 `7cm` 左右系统差是否稳定可校正。
3. 按 [轨迹模型数据采集流程](轨迹模型数据采集流程.md) 录制带已知信息的数据: 静态已知距离、固定距离前后/上下运动、来回拍球、正常击球。
4. 下载后先用 `trajectory_fusion/check_dataset.py` 检查行数、FPS、frame gap、水印 delta、P0/P1 有效率、median/MAD 和 `known_z` bias。
5. 多段数据写入 `dataset_manifest.yaml`，再用 `train_reliability.py` 训练可靠性模型，输出每个方法的 bias/sigma/outlier。
6. 用 `evaluate_fusion.py --json-out ... --csv-out ...` 比较:
   - P0 几何候选 median/weighted median
   - P0/P1 候选直接模型融合
   - reliability model consensus
   - Kalman 后轨迹
7. 只有当模型在静态真实距离和动态轨迹残差上都优于 P0 主路径，才考虑部署实时融合头。

## 当前默认结论

现在可以稳定进入 100fps 采集和训练主线的方法是:

```text
z_bbox_center
z_circle_center
z_roi_edge_centroid
z_roi_radial_center
z_roi_edge_pair_center
```

可作为 A/B 训练候选的方法是:

```text
z_roi_multi_point
z_roi_center_patch
```

当前默认采集字段冻结为上述 P0+P1 集合；P2 新算法可以有默认关闭字段和矩阵 case，但没有 NX isolated 结果前不得加入默认配置。

P2 待测字段当前只做 isolated A/B，不进入默认训练主线:

```text
z_roi_iou_region_color_patch
z_roi_patch_iou_color_edge
z_roi_cuda_template_match
z_roi_cuda_stereo_bm
z_roi_cuda_stereo_sgm
z_roi_orb_points
z_roi_brisk_points
z_roi_akaze_points
z_roi_sift_points
z_roi_neural_feature
```
