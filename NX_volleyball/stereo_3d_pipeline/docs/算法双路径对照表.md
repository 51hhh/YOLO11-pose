# 算法双路径对照表

基于当前代码、离线探针结果和 NX profiler 基线整理。此表用于约束后续实现:没有实时实现、没有 CSV 字段、没有通过 NX 性能门槛的算法不得写成在线可用。最新 NX 矩阵报告见 `test_logs/nx_algorithm_matrix_gpu_landing_final3_20260702/report.md`。

| 算法 | 离线路径 | 实时路径 | 默认开关 | 在线字段 / source | NX 性能状态 | 下一步 |
|---|---|---|---|---|---|---|
| bbox center / bbox edges | 已有 | GPU 几何候选 | 开 | `z_bbox_center`, `z_bbox_left_edge`, `z_bbox_right_edge`; source 3/6 | default_geometry 中 100 FPS | 保持基线 |
| circle center / circle edges | 已有 | GPU ROI 圆拟合候选 | 开 | `z_circle_center`, `z_circle_left_edge`, `z_circle_right_edge`; source 1 | default_geometry 中 100 FPS | 保持基线 |
| roi_edge_centroid | 已有 | GPU 几何候选 | 开 | `z_roi_edge_centroid`; source 5 | default_geometry 中 100 FPS | 保持基线 |
| roi_radial_center | 已有 | GPU 几何候选 | 开 | `z_roi_radial_center`; source 8 | default_geometry 中 100 FPS | 保持基线 |
| roi_edge_pair_center | 已有 | GPU 几何候选 | 开 | `z_roi_edge_pair_center`; source 9 | default_geometry 中 100 FPS | 保持基线 |
| roi_center_patch | 已有 | GPU ZNCC 候选 | 关 | `z_roi_center_patch`; source 4 | final3: 100.0 FPS, GPU avg 0.36 ms | 可单项 A/B，默认仍关 |
| roi_multi_point / subpixel | 已有 | GPU 多点亚像素候选，有时间预算闸 | 关 | `z_roi_multi_point`; source 2 | final3: 69.7 FPS, stale 114, GPU avg 3.59 ms | 质量 A/B，不作为 100Hz 默认项 |
| patch_iou_zncc_corner | 已有 | GPU `roi_corner_points` 快速候选 | 关 | `z_roi_corner_points`; source 10 | final3: 99.4 FPS, stale 2, GPU avg 0.78 ms | 继续做数值对拍 |
| texture points | 离线无一一对应摘要 | GPU `roi_texture_points` | 关 | `z_roi_texture_points`; source 11 | final3: 100.1 FPS, GPU avg 0.27 ms | 可单项 A/B |
| binary points | 离线无一一对应摘要 | GPU `roi_binary_points` | 关 | `z_roi_binary_points`; source 13 | final3: 99.8 FPS, GPU avg 0.53 ms | 可单项 A/B，默认关 |
| ORB | CPU OpenCV 探针 | true OpenCV CUDA ORB；CPU only fallback | 关 | `z_roi_orb_points`; source 14 | true CUDA ORB: 78-79 FPS, `Stage2_OpenCVCudaORB` avg 3.24 ms, 0/512 valid | 真实算法已接入，但质量和同步耗时不过关；VPI ORB 可另做后端 A/B |
| BRISK | CPU OpenCV 探针 | 当前 NX 无 true CUDA BRISK；历史 GPU-lite/CPU debug only | 关 | `z_roi_brisk_points`; source 15 | true GPU 不成立；历史 lite final3: 100.0 FPS, 0 valid | 不应宣称实时真实 GPU BRISK |
| AKAZE | CPU OpenCV 探针 | 当前 NX 无 true CUDA AKAZE；历史 GPU-lite/CPU debug only | 关 | `z_roi_akaze_points`; source 16 | true GPU 不成立；历史 lite final3: 100.1 FPS, 0 valid | 不应宣称实时真实 GPU AKAZE |
| SIFT | CPU/离线探针 | 当前 NX 无 true CUDA SIFT；历史 GPU-lite only | 关 | `z_roi_sift_points`; source 17 | true GPU 不成立；历史 lite final3: 100.1 FPS, 0 valid | 低收益候选，默认关 |
| iou_region_color_patch | 离线冠军，pass | GPU BGR label/patch 快速候选 | 关 | `z_roi_iou_region_color_patch`; source 18 | final3: 100.1 FPS, GPU avg 0.27 ms | 需与离线 LAB 版本做数值对拍 |
| patch_iou_color_edge | 离线有实现但失败 | GPU BGR edge/patch 快速候选 | 关 | `z_roi_patch_iou_color_edge`; source 19 | final3: 100.1 FPS, GPU avg 0.27 ms | 需复核有效性 |
| XFeat | Python 离线探针 | TensorRT extractor-only split 已接入；fused `[N,4/5]` 入口也支持 | 关 | `z_roi_neural_feature`; source 20 | XFeat 128/160/224 同步实测 75-90 FPS；有效候选稳定 | 需要 GPU 后处理/异步 FeatureJob 才能守 100fps |
| ALIKED | Python 离线探针 | fused `[N,4/5]` 通用入口支持；direct extractor schema 支持 | 关 | `z_roi_neural_feature`; source 20 | 未在实时 TensorRT 管线验证 | 需要真实 engine schema 对拍 |
| SuperPoint + LightGlue | Python 离线探针 | 支持 fused `[N,4/5]` 输出解析；direct extractor fallback 支持 | 关 | `z_roi_neural_feature`; source 20 | 未在实时 TensorRT 管线验证 | LightGlue split matcher 仍需 fused 或专用 runtime |

## 当前门槛

- 默认配置必须保持 default_geometry 级别: ROI FPS 约 100，`Stage2_DualYoloGpuCandidates` 均值约 0.26 ms，且无持续 stale drop。
- 新算法默认关闭；打开单项后必须记录 `Stage2_DualYoloGpuCandidates`、`Stage2_DualYoloMatch`、ROI FPS、stale drop。
- final3 中 `all_sparse_gpu` 为 89.8 FPS/stale 7/GPU avg 1.07 ms，不能把多种稀疏候选同时作为 100Hz 默认配置。
- offline 与 realtime 语义必须通过 `tools/compare_offline_online_depth.py` 对拍后才能进入优先级链或融合链。

## 对拍入口

当前可用的实时单帧入口:

```bash
./build/stereo_pipeline \
  --config config/pipeline_dual_yolo_roi.yaml \
  --debug-feature-matches \
  --debug-feature-matches-dir test_logs/feature_match_debug
```

当前可用的 CSV 对拍工具:

```bash
python3 tools/compare_offline_online_depth.py \
  --offline-summary test_logs/offline_keypoint_probe_recommended/summary.csv \
  --online-csv dual_yolo_observation_data.csv
```

注意: `--debug-feature-matches` 目前主要输出图像/ROI 调试材料,不是完整 extended CSV。要实现同一 stereo pair 的全自动闭环,后续应新增 `--probe-once` 或扩展 debug 输出为 `TrajectoryRecorder` 宽表。
